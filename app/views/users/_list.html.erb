
	<h2>User info</h2>
	<%= form_for(user) do |f| %>
	<% if book.errors.any? %>
	<h3><%= user.errors.count %>件のerrorが発生しました</h3>
<ul>
	<% user.errors.full_messages.each do |message| %>
		<%= message %>
	<% end %>
	<% end %>
</ul>

  <table class="table">
  <tbody>
  	<tr></tr>
  <tr>
	<th>name</th>
	<th><%= user.name %></th>
</tr>
<% end %>
<tr>
	<th>introduction </th>
	<th> <%= user.introduction %></th>
</tr>
</tbody>
</table>
	<%= link_to "",edit_user_path(user), class: "btn btn-default glyphicon glyphicon-wrench " %>

<h2>New book</h2>
<%= form_for(book) do |f| %>
	<% if book.errors.any? %>
	<h3><%= book.errors.count %>件のerrorが発生しました</h3>
<ul>
	<% book.errors.full_messages.each do |message| %>
		<%= message %>
	<% end %>
	<% end %>
</ul>


<h4>Title</h4>
<%= f.text_field :title %>
<h4>Opinion</h4>
<%= f.text_area :body %><br>

<%= f.submit 'Create Book'  %>

<% end %>

