<div class="container">
	<div class="row">
<div class="col-md-6">
<h1>Editing Book</h1>
</div>
<%= form_for(@book) do |f| %>
 <div class="field row">
<div class="col-xs-12">
<% if@book.errors.any? %>
<h2><%= @book.errors.count %>件のerrorが発生しました。</h2>
<% @book.errors.full_messages.each do |message| %>
<li><%= message %></li>
<% end %>
<% end %>
	<label>Title</label>
	<%= f.text_field :title %><br>
</div>
<div class="field row">
	<div class="col-xs-12">
	<label>Opinion</label>
	<%= f.text_area :body %><br>
</div>
</div>
<div class="actions row">
	<div class="col-xs-12">
<div class="btn">
	<%= f.submit 'Update Book' %>
	<% end %>
</div>
</div>
</div>
</div>

<td><%= link_to 'Show',book_path(@book) %></td>
<td><%= link_to 'Back',books_path(@books) %></td>
</div>
</div>

aaa